#!/bin/sh
# Pre-commit hook: 在commit前自动更新package.json的version为当前分支名

echo "🔄 正在更新版本号..."

# 运行版本更新脚本
node conf/update-version.js

# 检查脚本是否成功执行
if [ $? -ne 0 ]; then
    echo "❌ 版本更新失败，取消commit操作"
    exit 1
fi

echo "✅ 版本更新完成，继续commit..."
exit 0